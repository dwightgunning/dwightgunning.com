---
export interface Props {
  heading: string;
  description?: string;
}

const { description, heading } = Astro.props;
---

<li>
  <h4 class="font-semibold text-accent">{heading}</h4>
  <p class="mt-1">{description}</p>
</li>
